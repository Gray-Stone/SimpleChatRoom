ChatRoomGUI contains the GUI client. 
ChatRoomServer contains the server app of the chatroom 

Both are packed into jar and can be runned by double click or java -jar command from commandline. 

The full project is also stored on github : https://github.com/Gray-Stone/SimpleChatRoom